document.addEventListener('entity:changed',e=>{const{entity,action,message,status}=e.detail||{};const modalEl=document.getElementById('modal-container');const modal=bootstrap.Modal.getInstance(modalEl);if(modal)modal.hide();const toastEl=document.getElementById('toast-container');const toastMsg=document.getElementById('toast-body');toastMsg.textContent=message||`${entity} ${action} successful`;toastEl.className=`toast align-items-center border-0 text-bg-${status||'success'}`;bootstrap.Toast.getOrCreateInstance(toastEl).show()});document.addEventListener('htmx:responseError',function(event){document.getElementById('toast-body').textContent='Network error occurred. Please try again.';const toastEl=document.getElementById('toast-container');bootstrap.Toast.getOrCreateInstance(toastEl).show()});document.addEventListener('DOMContentLoaded',function(){document.addEventListener('hide.bs.modal',function(event){if(document.activeElement)document.activeElement.blur()})});